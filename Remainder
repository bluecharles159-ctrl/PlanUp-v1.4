fixes: {
    fab: 
        1. Fab deforming
        2. Replace the category input with store input. Category to be changed manually on item card after adding
        3. Maybe replace the smart sort feature with meal course
        4. Fix the concurrent item container for smooth transition
    
    notification: 
        1. Bell animating unwantedly

    feedbackPage:
        1. Text to begin at top
    
    toGetListPage:
        1. Back btn not clicking after opening toGetModal
        2. addFromRecipe btn not calling openRecipePage() 
     
    recipePreparationPage:
        1. Reference images to fit the their container

    toast:
        1. Toast in queue

    rangeText:
        1. rangeText btn not clickable after closing the rangeModal 

    chart:
        1. Bar chart to be vertical
        2. Bar chart to be extendible on the X to fit all items in the list
        3. Bar chart to have border radius and different colors then have a horizontal line on the head to trace to its value
        4. Doughnut chart to scale when its parent can't fit the whole width due to minimum screen width
        5. XM style line chart for item

    recipe:
        1. Recipe calendar to push recipe to scheduled list

    @place:
        1. To hold and separate data from different for different places of use for easy management of groceries


}

features: {
    insights: {
      overview => Period selector {
        <-Compare->
      }
      recipePreparationPage => Set timer on recipe {}
    }

}